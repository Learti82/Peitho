# Railway / Nixpacks build config for the Peitho parsing service.
# Installs Tesseract OCR (with Albanian + English language data) for scanned PDFs.

[phases.setup]
aptPkgs = ["tesseract-ocr", "tesseract-ocr-sqi", "tesseract-ocr-eng"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
